export type Track = {
}

export type Base = {
    position: Position,
    size: Size,
}

export type Position = {
    x: number,
    y: number,
}

export type Size = {
    width: number,
    height: number,
}